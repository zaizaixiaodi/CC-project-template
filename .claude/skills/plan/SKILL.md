---
name: plan
description: 创建与维护 PLAN.md 任务协议（目标、排除范围、实施步骤、验收标准、验证手段），任务完成后归档到 archive/plans/。适用于：用户调用 /plan、用户提出看起来较复杂的新需求时（先询问用户是否立协议，不要擅自创建）、或开发中计划发生方向性变更时。不适用于：简单直接的任务，或会反复执行的固定流程（应沉淀为专属 skill）。
disable-model-invocation: false
argument-hint: <任务名，可选>
---

# plan

PLAN.md 是**任务协议**：开工前把目标、边界、验收标准写死，防止"看起来做完了"的静默失败。它管一次任务的"做什么、做到什么程度"；session 之间的接力由 HANDOFF.md 管。

## 何时创建

创建靠**问，不靠猜**——"是否跨 session"无法事前预判，不要替用户决定。三个触发点：

1. **用户主动调用 `/plan`** → 直接建（从 `templates/PLAN.md` 复制到根目录）。
2. **用户提出新需求且有复杂信号**（要搭建 workflow/小产品、交付物含多个组成部分、需要验收标准才能判断完成）→ **先问用户**"这个任务要不要立一个 PLAN.md"，确认后再建。
3. **会话收尾兜底**：handoff/done 时任务没做完且根目录无 PLAN.md → 此刻"跨 session"已成事实，提示用户补建。

不建的情况：简单直接的任务；重复性流程（第二次做同类任务时，考虑把它沉淀成专属 skill，PLAN.md 只留个性化部分）。

读取不需要判断：PLAN.md **存在本身就是状态标记**——会话开始见根目录有它就读（CLAUDE.md 已约定），无需额外状态文件。

## 与 HANDOFF.md 的分工

| | PLAN.md | HANDOFF.md |
|---|---|---|
| 粒度 | 任务级 | session 级 |
| 管什么 | 目标、边界、验收标准、步骤进度 | 在途状态、下一步动作、决策、踩坑 |
| 进度勾在哪 | 实施步骤的 checkbox | 不勾，计划段只留一行指针 |

PLAN.md 存在时，HANDOFF.md 的"计划 (Plan)"段**只写一行**：`见 PLAN.md（当前在第 N 步）`，不要两处维护。

## 变更纪律（核心）

计划在开发中变更是常态，按性质分两级处理：

- **方向性变更**（目标、不做什么、验收标准变了）：直接把 PLAN.md **改到新状态**，同时在 HANDOFF.md 的"在途决策"记一行——`计划变更：改 X 弃 Y，因为 Z`。来龙去脉就靠这一行 + git 历史，**不另设 devlog**。
- **细节性变更**（步骤拆分调整、补充验证项）：直接改，不留痕。git 兜底。

## 完成归档

验收标准全部达成后：

1. 有沉淀价值的内容先迁出：可复用的验证清单/踩坑 → 按 handoff skill 的冲刷规则分流（CLAUDE.md / rules / docs / 专属 skill）。
2. 把 PLAN.md 移动到 `archive/plans/<YYYY-MM-DD>-<任务名>.md`（目录不存在则创建）。
3. 更新 HANDOFF.md：计划段的指针行删除或换成下一任务。

## 硬约束

- PLAN.md ≤ **100 行**；根目录同时只允许**一个**活跃 PLAN.md。
- 验收标准每条必须**5 秒内可判断是否达成**（"条款逐条核对完毕"✓，"质量要高"✗）。
- 验证手段不限于命令：核对清单、交叉比对、人工确认项都算，但必须写明**具体怎么查**。
